import 'package:flutter/material.dart';

/// 猿星球
class ApePlanetPage extends StatelessWidget {
  const ApePlanetPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("ApePlanetPage"),
    );
  }
}